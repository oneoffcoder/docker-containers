# Notes

## Testing

```bash
# test java
which java
java -version

# test hadoop
hadoop version

# test spark
spark-submit --version

# test python
which python
python --version

# test ssh
ssh localhost

# test listening ports
netstat -tulnp | grep LISTEN
```